import { refreshTokenSchema } from '#student-360/student/infrastructure/config/schema/refresh-token.schema';
import { studentRecoveryPasswordTokenSchema } from '#student-360/student/infrastructure/config/schema/student-recovery-password-token.schema';

export const schemas = [refreshTokenSchema, studentRecoveryPasswordTokenSchema];
